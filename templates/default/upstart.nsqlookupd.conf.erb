description "nsqlookupd"

start on runlevel [2345]
stop on runlevel [!2345]

env PATH=/usr/local/sbin:/usr/local/bin:/usr/bin:/usr/sbin:/sbin:/bin

respawn

script
  exec su -s /bin/sh -c 'exec "$0" "$@"' nsqlookupd -- /usr/local/bin/nsqlookupd 2>&1
end script
